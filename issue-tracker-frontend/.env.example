# Copy to ".env" for local `npm run dev` / `npm run build` (see .env.production.example for prod builds).
# Docker: prefer --build-arg VITE_API_BASE_URL=... (see Dockerfile, docker-compose.yml).
VITE_API_BASE_URL=http://localhost:9092/api
