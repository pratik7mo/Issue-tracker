# Copy to ".env.production" before `npx vite build --mode production` (CI or bare-metal).
# Example for a deployed API host (must include /api or axios will normalize it):
VITE_API_BASE_URL=http://YOUR_PUBLIC_IP_OR_HOST:9092/api
