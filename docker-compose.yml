services:
  backend:
    build:
      context: ./issue-tracker-backend
      dockerfile: Dockerfile
    ports:
      - "9092:8080"
    environment:
      DB_URL: jdbc:postgresql://host.docker.internal:5432/issue_tracker
      DB_USERNAME: postgres
      DB_PASSWORD: ${DB_PASSWORD:-7894}

  frontend:
    build: ./issue-tracker-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
